import styled from "styled-components";
import previousImg from '../../public/previous.svg'

export const Previous = styled.div`
  content url(${previousImg});
  width: 35px;
  height: 35px;
  cursor: pointer;

`